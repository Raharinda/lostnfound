Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7

# Database support (SQLite already built-in, but add SQLAlchemy if expanded later)
SQLAlchemy==2.0.32

# Optional but recommended for security and file handling
python-dotenv==1.0.1

# For image upload validation (optional)
Pillow==10.4.0
